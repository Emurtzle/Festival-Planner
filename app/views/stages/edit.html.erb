<h1>Edit the Stage at <%= @fest.name %></h1>

<%= form_for @stage, url: "/festival/#{@fest.id}/stages/#{@stage.id}" do |f|%>
  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :description %>
  <%= f.text_field :description %><br>

  <%= f.submit "Edit Stage" %>
<% end %>
